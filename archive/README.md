This directory holds archived project material that is not part of the live game path.

Contents:
- `legacy-click-ui/`
  The older browser client and related assets kept for reference only.
- `legacy-stories/`
  Replaced story folders that are no longer bound to live tiles.

Live story content stays under `/stories`.
Live click UI files stay at the repo root and under `/api`, `/story`, and `/state`.
